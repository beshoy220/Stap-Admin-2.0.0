import 'package:flutter/material.dart';

class SetExamsMarksScreen extends StatelessWidget {
  const SetExamsMarksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Set exams marks'),
      ),
    );
  }
}
